{
  "name": "mydevnotes",
  "displayName": "My Dev Notes",
  "description": "Workspace-scoped TODO list and scratch pad with groups, archive, markdown notes, and MCP server",
  "version": "0.9.16",
  "publisher": "tristancartledge",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/reefbarman/mydevnotes"
  },
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "keywords": [
    "todo",
    "task",
    "notes",
    "scratch pad",
    "markdown",
    "productivity",
    "mcp",
    "claude"
  ],
  "activationEvents": [
    "onView:mydevnotes.mainView",
    "onCommand:mydevnotes.addTodo"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "mydevnotes-sidebar",
          "title": "My Dev Notes",
          "icon": "$(notebook)"
        }
      ]
    },
    "views": {
      "mydevnotes-sidebar": [
        {
          "type": "webview",
          "id": "mydevnotes.mainView",
          "name": "My Dev Notes"
        }
      ]
    },
    "commands": [
      {
        "command": "mydevnotes.addTodo",
        "title": "Add TODO",
        "category": "My Dev Notes",
        "icon": "$(add)"
      },
      {
        "command": "mydevnotes.focusSearch",
        "title": "Focus Search",
        "category": "My Dev Notes",
        "icon": "$(search)"
      },
      {
        "command": "mydevnotes.reveal",
        "title": "Reveal Current Task",
        "category": "My Dev Notes"
      }
    ],
    "configuration": {
      "title": "My Dev Notes",
      "properties": {
        "mydevnotes.mcpAutoRegister": {
          "type": "string",
          "enum": [
            "enabled",
            "disabled",
            "ask"
          ],
          "default": "ask",
          "description": "Automatically register the MCP server for AI agents (e.g. Claude Code) in each workspace."
        }
      }
    },
    "menus": {
      "view/title": [
        {
          "command": "mydevnotes.addTodo",
          "when": "view == mydevnotes.mainView",
          "group": "navigation"
        },
        {
          "command": "mydevnotes.focusSearch",
          "when": "view == mydevnotes.mainView",
          "group": "navigation"
        }
      ]
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile && npm run bundle:mcp && npm run bundle:webview",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "bundle:mcp": "esbuild mcp-server.js --bundle --platform=node --target=node18 --outfile=out/mcp-server.js --format=cjs",
    "bundle:webview": "esbuild webview/App.tsx --bundle --outfile=out/webview.js --format=iife --target=es2020 --jsx=automatic --jsx-import-source=preact --external:vscode",
    "package": "bash scripts/release.sh",
    "package:raw": "vsce package --no-dependencies",
    "reinstall": "bash scripts/release.sh --install",
    "release": "bash scripts/release.sh",
    "release:install": "bash scripts/release.sh --install"
  },
  "dependencies": {
    "@vscode/codicons": "^0.0.45",
    "marked": "^15.0.0",
    "preact": "^10.25.0"
  },
  "devDependencies": {
    "@modelcontextprotocol/sdk": "^1.26.0",
    "@types/node": "^20.0.0",
    "@types/vscode": "^1.85.0",
    "esbuild": "^0.27.3",
    "typescript": "^5.3.0"
  }
}
